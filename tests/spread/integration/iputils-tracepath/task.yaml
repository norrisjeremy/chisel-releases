summary: Integration tests for iputils-tracepath

execute: |
  # Chisel a minimum number of slices to give us a runnable system that we can
  # test in.
  rootfs="$(install-slices iputils-tracepath_bins)"

  chroot "${rootfs}/" tracepath -V
